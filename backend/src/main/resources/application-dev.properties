# ─────────────────────────────────────────────────────────────
#  Nudge — Development Profile
#  Activate with: -Dspring.profiles.active=dev
#  or set SPRING_PROFILES_ACTIVE=dev
# ─────────────────────────────────────────────────────────────

# Dev secrets (NEVER use these in production)
DB_PASSWORD=password
JWT_SECRET=ZGV2T25seVNlY3JldEtleUZvck51ZGdlTG9jYWxEZXZlbG9wbWVudA==
ENCRYPTION_KEY=ZGV2T25seUVuY3J5cHRpb25LZXlGb3JOdWRnZUxvY2Fs

# Hibernate creates/updates tables in dev; Flyway is disabled
JPA_DDL_AUTO=update
FLYWAY_ENABLED=false

# All origins allowed in dev (file://, localhost, extension)
CORS_ALLOWED_ORIGINS=*

# Email notifications off by default in dev
NOTIFICATION_EMAIL_ENABLED=false

# Debug logging in dev
logging.level.com.nudge=DEBUG
logging.level.org.springframework.security=DEBUG
spring.jpa.show-sql=true
